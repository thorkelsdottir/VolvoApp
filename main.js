import Exponent from 'exponent';
import React from 'react';
import App from './api';

class MainMotherFucker extends React.Component {
  render() {
    return(
      <App/>
    );
  }
}

Exponent.registerRootComponent(MainMotherFucker);
